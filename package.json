{
  "name": "static-scss-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:css": "npx --yes sass src/scss:dist/css --no-source-map",
    "build": "npm run build:css",
    "vercel-build": "npm run build"
  },
  "devDependencies": {
    "sass": "^1.80.0"
  }
}